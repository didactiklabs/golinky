apiVersion: v1
kind: Service
metadata:
  name: golinky
  namespace: golinky
  labels:
    app: golinky
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 80
    targetPort: http
  selector:
    app: golinky
